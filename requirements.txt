# Versions are pinned for reproducible deployments.
# Run `pip install -r requirements.txt` to install.
flask==3.0.3
flask-socketio==5.3.6
python-dotenv==1.0.1
requests==2.31.0
eventlet==0.36.1
Markdown==3.6
