# Task List — Use gh copilot suggest to complete these!

## Task 1
List all files in this directory, including hidden files, with human-readable sizes.

## Task 2
Count how many lines are in each of the `.txt` files here.

## Task 3
Find all files that were modified in the last 10 minutes.

## Task 4
Create a compressed archive of the `data/` folder named `backup.tar.gz`.

## Task 5
Display the first 5 lines of every `.txt` file in the `data/` directory.

## Task 6
Sort the words in `words.txt` alphabetically and remove duplicates, saving the result to `sorted_words.txt`.

## Task 7
Count how many times the word "error" appears in `server.log` (case-insensitive).

## Task 8
Replace all occurrences of "TODO" with "DONE" in `notes.txt`.
